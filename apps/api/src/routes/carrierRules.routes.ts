import { Router } from 'express'; import { parseTrackingText } from '@voraco/shared'; import { deleteRow, insertRow, listRows, updateRow } from '../services/crud.js';
export const carrierRulesRoutes = Router(); carrierRulesRoutes.get('/', async (_req,res)=>res.json(await listRows('carrier_rules'))); carrierRulesRoutes.post('/', async (req,res)=>res.status(201).json(await insertRow('carrier_rules',req.body))); carrierRulesRoutes.put('/:id', async (req,res)=>res.json(await updateRow('carrier_rules',req.params.id,req.body))); carrierRulesRoutes.delete('/:id', async (req,res)=>res.json(await deleteRow('carrier_rules',req.params.id))); carrierRulesRoutes.post('/test-parser', (req,res)=>res.json(parseTrackingText(req.body.sampleText ?? '', req.body.rule ?? req.body)));
