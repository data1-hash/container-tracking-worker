import { IMPORT_MILESTONES, type Shipment } from '@voraco/shared'; import { ShipmentTable } from '../components/ShipmentTable'; export function ImportShipments({ shipments, onOpen }: { shipments: Shipment[]; onOpen:(id:string)=>void }) { return <div className="space-y-4"><div className="card"><h2 className="font-bold">Import workflow</h2><p className="text-sm text-slate-500">{IMPORT_MILESTONES.join(' → ')}</p></div><ShipmentTable shipments={shipments.filter((s)=>s.shipment_direction==='IMPORT')} onOpen={onOpen}/></div>; }
