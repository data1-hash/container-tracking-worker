import type { Alert } from '@voraco/shared'; import { StatusBadge } from '../components/StatusBadge'; export function Alerts({ alerts }: { alerts: Alert[] }) { return <div className="card"><h2 className="font-bold">Open Alerts</h2>{alerts.map((a)=><div key={a.id} className="mt-3 flex justify-between rounded-xl border p-3"><span>{a.message}</span><StatusBadge value={a.severity}/></div>)}</div>; }
