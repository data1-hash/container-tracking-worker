import { EXPORT_MILESTONES, type Shipment } from '@voraco/shared'; import { ShipmentTable } from '../components/ShipmentTable'; export function ExportShipments({ shipments, onOpen }: { shipments: Shipment[]; onOpen:(id:string)=>void }) { return <div className="space-y-4"><div className="card"><h2 className="font-bold">Export workflow</h2><p className="text-sm text-slate-500">{EXPORT_MILESTONES.join(' → ')}</p></div><ShipmentTable shipments={shipments.filter((s)=>s.shipment_direction==='EXPORT')} onOpen={onOpen}/></div>; }
