import { StatCard } from '../components/StatCard'; import type { DashboardSummary } from '@voraco/shared';
export function Dashboard({ summary }: { summary: DashboardSummary }) { const cards = [['Total active shipments', summary.totalActive], ['Import shipments', summary.imports], ['Export shipments', summary.exports], ['Delayed shipments', summary.delayed], ['Arriving in 7 days', summary.arrivingThisWeek], ['Manual review count', summary.manualReviews], ['Pending tracking jobs', summary.pendingJobs], ['Critical alerts', summary.criticalAlerts]] as const; return <div className="space-y-6"><div className="grid gap-4 md:grid-cols-4">{cards.map(([l,v])=><StatCard key={l} label={l} value={v}/>)}</div><div className="grid gap-4 lg:grid-cols-3"><div className="card"><h2 className="font-bold">Import vs Export</h2><div className="mt-4 flex gap-2"><div className="h-24 flex-1 rounded bg-blue-500"/><div className="h-24 flex-1 rounded bg-emerald-500"/></div></div><div className="card"><h2 className="font-bold">Status distribution</h2><div className="mt-4 h-24 rounded-xl bg-gradient-to-r from-emerald-200 via-amber-200 to-red-200"/></div><div className="card"><h2 className="font-bold">Monthly shipment count</h2><div className="mt-4 flex h-24 items-end gap-2">{[30,55,40,75,65].map((h,i)=><div key={i} className="flex-1 rounded bg-blue-600" style={{height:h}} />)}</div></div></div></div>; }
