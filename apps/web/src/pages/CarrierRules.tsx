import type { CarrierRule } from '@voraco/shared'; import { CarrierRuleForm } from '../components/CarrierRuleForm'; export function CarrierRules({ rules }: { rules: CarrierRule[] }) { return <div className="space-y-4"><CarrierRuleForm/><div className="card">{rules.map((r)=><p key={r.id} className="mt-2"><b>{r.carrier}</b> · {r.fetch_mode} · {r.tracking_url_pattern}</p>)}</div></div>; }
