import type { Shipment } from '@voraco/shared'; import { ShipmentTable } from '../components/ShipmentTable';
export function Shipments({ shipments, onOpen }: { shipments: Shipment[]; onOpen: (id:string)=>void }) { return <div className="space-y-4"><div className="card"><div className="grid gap-3 md:grid-cols-6"><input className="input md:col-span-2" placeholder="Search shipment, BL, container, booking, customer"/><select className="input"><option>All directions</option><option>IMPORT</option><option>EXPORT</option></select><select className="input"><option>Status</option></select><select className="input"><option>Carrier</option></select><button className="btn">Add shipment</button></div></div><ShipmentTable shipments={shipments} onOpen={onOpen}/></div>; }
