import type { ManualReview } from '@voraco/shared'; export function ManualReviewPanel({ reviews }: { reviews: ManualReview[] }) { return <div className="card"><h2 className="font-bold">Manual Review</h2>{reviews.map((r)=><div key={r.id} className="mt-3 rounded-xl border p-3"><p className="font-semibold">{r.carrier} · {r.tracking_number}</p><p className="text-sm text-slate-600">{r.reason}</p><textarea className="input mt-3 w-full" placeholder="Paste visible carrier tracking text for parser preview" /></div>)}</div>; }
