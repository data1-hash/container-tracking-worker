import type { DocumentRecord } from '@voraco/shared'; import { StatusBadge } from './StatusBadge'; export function DocumentList({ documents }: { documents: DocumentRecord[] }) { return <div className="card"><h2 className="font-bold">Documents</h2>{documents.map((d)=><div key={d.id} className="mt-3 flex justify-between rounded-xl border p-3"><span>{d.document_type} {d.document_no}</span><StatusBadge value={d.status}/></div>)}</div>; }
