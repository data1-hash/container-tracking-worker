import type { TrackingJob } from '@voraco/shared'; import { StatusBadge } from './StatusBadge'; export function QueuePanel({ jobs }: { jobs: TrackingJob[] }) { return <div className="card"><h2 className="font-bold">Tracking Queue</h2>{jobs.map((j)=><div key={j.id} className="mt-3 flex justify-between rounded-xl border p-3"><span>{j.carrier} · {j.tracking_number} · attempts {j.attempts}</span><StatusBadge value={j.job_status}/></div>)}</div>; }
