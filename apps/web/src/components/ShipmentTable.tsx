import type { Shipment } from '@voraco/shared'; import { formatDate } from '../lib/utils'; import { StatusBadge } from './StatusBadge';
export function ShipmentTable({ shipments, onOpen }: { shipments: Shipment[]; onOpen?: (id:string)=>void }) { return <div className="card overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="text-slate-500"><th className="p-3">Shipment</th><th>Direction</th><th>Customer/Supplier</th><th>Carrier</th><th>ETA</th><th>Status</th><th>Risk</th></tr></thead><tbody>{shipments.map((s)=><tr key={s.id} onClick={()=>onOpen?.(s.id)} className="cursor-pointer border-t hover:bg-slate-50"><td className="p-3 font-semibold">{s.shipment_no}<div className="text-xs text-slate-500">{s.bl_no ?? s.booking_no}</div></td><td>{s.shipment_direction}</td><td>{s.customer_name}<div className="text-xs text-slate-500">{s.supplier_name}</div></td><td>{s.carrier}</td><td>{formatDate(s.eta)}</td><td><StatusBadge value={s.current_status}/></td><td><StatusBadge value={s.risk_status}/></td></tr>)}</tbody></table></div>; }
